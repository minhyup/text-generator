import React from "react";
import MenuLayout from "@/components/layout/MenuLayout";

function join() {
  return (
    <MenuLayout>
      <span>로그인</span>
    </MenuLayout>
  );
}

export default join;
